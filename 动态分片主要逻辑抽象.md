##     动态分片

```js
并发量为`K`，要求最多同时执行`N`个任务。
当前正在执行`A`个任务（`A <= N`），则这`A`个任务的并发量为`K`。

如果`A = 0`,按照数学逻辑给`N`个任务分配`默认值`。
例如，2，2，2


如果`A > 0`,伴随着并发将前`A`匀给剩下`N-A`个任务。 
例如，6，
     5，1
     4，1，1 
     3，2，1
     3，1，1----->3,1,2

```

